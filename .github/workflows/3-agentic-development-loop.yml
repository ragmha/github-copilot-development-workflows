name: Step 3 - Agentic development loop

on:
  push:
    branches: [main]
    paths:
      - "src/**"
      - "tests/**"

permissions:
  contents: read
  issues: write

concurrency:
  group: step-3-${{ github.ref }}
  cancel-in-progress: true

jobs:
  post-next-step:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Post next step
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          NEXT_STEP_FILE: .github/steps/3-agentic-development-loop.md
        run: |
          set -euo pipefail
          issue="$(gh issue list --repo "$REPO" --label exercise --state open --json number --jq '.[0].number // empty')"
          if [ -n "$issue" ]; then
            gh issue comment "$issue" --repo "$REPO" --body-file "$NEXT_STEP_FILE"
          fi
